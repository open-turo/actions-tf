name: "terraform Lint"
description: "Lints terraform repository"
inputs:
  checkout-repo:
    required: false
    description: "Perform checkout as first step of action"
    default: "true"
  github-token:
    required: true
    description: "GitHub token that can checkout the repository. e.g. 'secrets.GITHUB_TOKEN'"
  terraform-cli-credentials-token:
    required: true
    description: "The terraform cli config credentials token"
runs:
  using: composite
  steps:
    - name: Checkout
      uses: actions/checkout@v2
      if: inputs.checkout-repo
      with:
        fetch-depth: 0
    - name: Setup terraform
      uses: hashicorp/setup-terraform@v1
      with:
        terraform_version: 1.1.2
        cli_config_credentials_token: ${{ inputs.terraform-cli-credentials-token }}
    - name: Pre-commit
      uses: open-turo/action-pre-commit@use-open-turo-commitlint-config-conventional
